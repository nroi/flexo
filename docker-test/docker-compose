#!/bin/bash

docker-compose down

tar -C .. -czvf flexo-server/flexo.tar.gz flexo/src flexo/Cargo.toml

/usr/bin/docker-compose build || exit 1
/usr/bin/docker-compose up --exit-code-from flexo-client
